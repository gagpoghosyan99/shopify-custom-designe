<div class="nm-trust-pill nm-glass nm-glow-hover" {{ block.shopify_attributes }}>
  <span class="nm-trust-pill__icon" aria-hidden="true">{{ icon }}</span>
  <span class="nm-trust-pill__label">{{ label }}</span>
</div>

